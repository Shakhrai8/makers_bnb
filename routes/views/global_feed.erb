<!DOCTYPE html>
<html>
<head>
  <title>MAKERS BnB - Global Feed</title>
  <link rel="stylesheet" type="text/css" href="/feed_style.css">
</head>
<body>
  <div class="container">
    <div class="menu">
      <a href="/profile" class="profile-button">My Profile</a>
      <a href="/new_space" class="new-space-button">List a Space</a>
      <a href="/logout" class="logout-button">Log Out</a>
    </div>
    <div class="content">
      <div class="header">
        <h1 class="heading">Global Feed</h1>
      </div>
      <div class="feed">
        <% @spaces.each do |space| %>
          <div class="space-card">
            <h2><%= space.name %></h2>
            <p><%= space.description %></p>
            <p>£<%= space.price %></p>
            <a href="/space/<%= space.id %>" class="book-button">Book Now</a>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</body>
</html>
